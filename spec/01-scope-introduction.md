# 1. Scope and Introduction
> **Status:** informative · **Maturity:** n/a (framing)  
> **Rule-IDs:** none (framing/derived)  
> **Primary sources (explorations/):** claude-notes.md (Goals; 'primary spec is minimal'), differences-with-go.md, detailed-notes §30/§31  
> Full chapter scope & status caveats: `explorations/plan-language-spec.md` §5.

> _Stub — not yet authored._ Author with the per-construct rubric in [`conventions.md`](conventions.md) (Grammar → Constraints → Static semantics → Dynamic semantics → Exceptions → Notes/Examples). Every normative statement carries a stable rule-ID under the prefix above — the citation target for spec conformance tests.

## Scope notes

- Core language spec, INCLUDING tier-0 packages (Ch.20). Stdlib (tier 1) is a separate dependent sibling spec.
- Less-monolithic philosophy: no printf/stdlib in the core; language usable without it. pkg/bootstrap is temporary, not part of the language.
- Finalize LAST (after the body settles in/out of scope).

## Rules

_TODO._
