(* Binate Formal Grammar — canonical source of truth for the specification. *)
(* Annex A and the per-section inlined productions are generated from THIS file. *)
(*                                                                              *)
(* STATUS: placeholder. To be seeded from explorations/grammar.ebnf via the     *)
(* Phase-0 reconciliation pass (strip [BOOTSTRAP]/[DEFERRED]; const -> readonly; *)
(* complete the keyword/builtin list incl. make_slice/sizeof/alignof/same/      *)
(* present/unsafe_div/unsafe_rem; de-defer floats; FuncType -> *func/@func; drop *)
(* the stale 'enum' form and '3-word' managed-slice text; reconcile D5).         *)
(* explorations/grammar.ebnf is retired once this becomes canonical (D4).        *)
