# 4. Notation
> **Status:** normative · **Maturity:** Draft (apparatus — author first)  
> **Rule-ID prefix:** `notation`  
> **Primary sources (explorations/):** grammar.ebnf header; ECMA-55 / Rust Reference / Wasm (per survey)  
> Full chapter scope & status caveats: `explorations/plan-language-spec.md` §5.

> _Stub — not yet authored._ Author with the per-construct rubric in [`conventions.md`](conventions.md) (Grammar → Constraints → Static semantics → Dynamic semantics → Exceptions → Notes/Examples). Every normative statement carries a stable rule-ID under the prefix above — the citation target for spec conformance tests.

## Scope notes

- The ISO-14977-flavored EBNF metalanguage; the per-construct rubric; normative/informative discipline; the four-value status legend; the stable rule-ID scheme; a light operational-rule notation.
- Canonical grammar moves into the spec as binate.ebnf (D4); strip [BOOTSTRAP]/[DEFERRED] in Phase-0 reconciliation.
- Do NOT reuse [BOOTSTRAP]/[DEFERRED] as a stability axis.

## Rules

_TODO._
